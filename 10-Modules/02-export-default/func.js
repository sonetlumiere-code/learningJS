// export default

export default function(x) {
    return x + x;
}

// Uses the JavaScript export statement to export JavaScript objects, functions, and primitive data types (it can only ever have one default export).